{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "UV run (python)",
            "type": "shell",
            "command": "uv",
            "args": [
                "run",
                "${file}"
            ],
            "group": "build",
            "problemMatcher": [],
            "detail": "Run the current Python file with uv"
        }
    ]
}